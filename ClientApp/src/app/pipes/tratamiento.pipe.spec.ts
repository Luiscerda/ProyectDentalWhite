import { TratamientoPipe } from './tratamiento.pipe';

describe('TratamientoPipe', () => {
  it('create an instance', () => {
    const pipe = new TratamientoPipe();
    expect(pipe).toBeTruthy();
  });
});
