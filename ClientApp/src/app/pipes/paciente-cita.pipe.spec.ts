import { PacienteCitaPipe } from './paciente-cita.pipe';

describe('PacienteCitaPipe', () => {
  it('create an instance', () => {
    const pipe = new PacienteCitaPipe();
    expect(pipe).toBeTruthy();
  });
});
