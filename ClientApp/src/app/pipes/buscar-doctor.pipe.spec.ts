import { BuscarDoctorPipe } from './buscar-doctor.pipe';

describe('BuscarDoctorPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarDoctorPipe();
    expect(pipe).toBeTruthy();
  });
});
